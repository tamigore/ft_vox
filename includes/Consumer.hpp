#ifndef __CONSUMER_HPP__
# define __CONSUMER_HPP__

#include <map>
#include <thread>
#include <mutex>

# include "Chunk.hpp"

// class Consumer
// {
// 	public:
// 		std::mutex	&data_mutex;
// 		int			renderDistance;
// 		math::vec3	cameraPos;

// 		Consumer();
// 		Consumer(std::mutex &chunk_mutex);
// 		~Consumer();

// 		void	consume(std::map<std::pair<int, int>, obj::Chunk *> *data);
// };

#endif
